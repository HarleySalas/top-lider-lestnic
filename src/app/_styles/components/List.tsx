import { ThemeIcon, Flex } from "@mantine/core";
import { IconPoint } from "@tabler/icons-react";

export const listProps = {
  //enabling default icon here overrides ordered list numbers with this icon.
  // icon: (
  //   <ThemeIcon radius="xl" size={26} variant="transparent">
  //     <IconPoint stroke={2} />
  //   </ThemeIcon>
  // ),
  styles: { itemWrapper: { alignItems: "flex-start" } },
};
